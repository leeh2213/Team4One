{
  "name": "team-agent-collaboration-space",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @team4one/api --filter @team4one/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "e2e": "TMPDIR=/tmp COREPACK_HOME=/tmp/corepack PNPM_HOME=/tmp/pnpm-home pnpm --config.virtual-store-dir=/tmp/team4one-pnpm-virtual-store --filter @team4one/api seed && PLAYWRIGHT_BROWSERS_PATH=/tmp/ms-playwright playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
